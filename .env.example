# Local development uses SQLite by default.
DATABASE_URL="file:./dev.db"

# Set these in production to protect the website.
# Leave unset locally to skip password protection.
BASIC_AUTH_USERNAME=""
BASIC_AUTH_PASSWORD=""
